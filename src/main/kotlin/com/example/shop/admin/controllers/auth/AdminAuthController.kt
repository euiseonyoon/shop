package com.example.shop.admin.controllers.auth

import com.example.shop.constants.ADMIN_AUTH_URI_PREFIX

class AdminAuthController {
    companion object {
        const val URI = ADMIN_AUTH_URI_PREFIX
    }
}
